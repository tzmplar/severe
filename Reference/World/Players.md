# Players

The Players service manages all players in the game.

## LocalPlayer

### Definition

```luau
Players.LocalPlayer: Player?
```

### Description

Returns the local player (the player running the script), or `nil` if accessed from the server (read-only).
