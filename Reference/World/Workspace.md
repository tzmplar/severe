# Workspace

The Workspace service contains all of the physical objects in the game world.

## CurrentCamera

### Definition

```luau
workspace.CurrentCamera: Camera?
```

### Description

The current camera being used for rendering, or `nil` if no camera is set (read-only).
