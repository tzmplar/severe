# Model

A container object that groups together parts and other instances.

## PrimaryPart

### Definition

```luau
Model.PrimaryPart: BasePart?
```

### Description

The primary part of the model, which serves as the reference point for model operations, or `nil` if no primary part is set (read-only).
